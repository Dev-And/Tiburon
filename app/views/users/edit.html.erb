<% provide(:title, 'Edit user') %>
<h1 id="upd">Обновление профиля</h1>
<div class="row">
<div class="col-md-4 col-md-offset-4">
<%= form_for @user, :html => {:class => "form-horizontal "} do |f| %>
    <%=render 'fields', f:f %>

      <%= f.submit "Соранить изменения", class: "btn btn-large btn-primary" %>

        <div id="grav_edit">

        <%= gravatar_for @user %>
        <a href="http://gravatar.com/emails">change</a>
        </div>
    </div>
  <% end %>
  </div>

